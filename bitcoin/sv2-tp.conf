chain=regtest

# Connect to bitcoin-node via the IPC unix socket it creates under the data dir.
ipcconnect=unix

# Template interval and minimum fee delta (satoshis) before pushing a new template.
sv2interval=30
sv2feedelta=1000

debug=sv2
loglevel=sv2:trace
